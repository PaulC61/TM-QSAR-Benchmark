"""TM-QSAR-Benchmark: a consolidated benchmarking pipeline comparing the
Tsetlin Machine (TM) against Random Forest and XGBoost on QSAR datasets.

See `tm_qsar_benchmark.pipeline.run_benchmark` for the main entry point, or
run `python -m tm_qsar_benchmark.cli --help` from the command line.
"""

__version__ = "0.1.0"
